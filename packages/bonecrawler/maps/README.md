# Maps

Map data will move here as the editor/package contract stabilizes.

For now, `game.manifest.json` uses symbolic `runtime:` references that point to current JavaScript map modules under `src/maps/zones/`.
